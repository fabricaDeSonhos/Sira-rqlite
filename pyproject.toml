[project]
name = "Sira"
description = "Um sistema de reservas robusto, moderno e altamente escalável construído sobre uma arquitetura de banco de dados distribuída."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
