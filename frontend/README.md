# Sistema de Reservas - React

Protótipo frontend em React com dados estáticos.

## Rodar

```bash
npm install
npm run dev
```

> Não rode `npm audit fix --force`, porque ele troca versões do Vite e pode quebrar o projeto no Windows.
